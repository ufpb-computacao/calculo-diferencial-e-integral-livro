== Aplicações da Derivada

.Objetivos do capítulo
____________________
Ao final deste capítulo você deverá ser capaz de:

* Estabelecer a relação entre  o crescimento ou decrescimento   de uma função em um intervalo;
* Aplicar o critério da primeira e segunda derivada de uma função para encontrar os máximos e mínimos relativos à função;
* Encontrar os pontos de inflexão, se existem, e os intervalos onde a função é concava para cima e concava para baixo;
* Desenhar o gráfico de uma função depois de analisar seu domínio e imagem, intervalos onde a função é crescente ou decrescente, pontos máximos e mínimos da função , os pontos de inflexão, a concavidade;
* Aprender  regra de L'Hôpital, para determinar os valores de limites indeterminados da forma latexmath:[$0/0$], latexmath:[$\infty/\infty$], latexmath:[$\infty\cdot 0$], latexmath:[$\infty-\infty$], latexmath:[$1^\infty$], latexmath:[$0^0$] e latexmath:[$\infty^0$].
____________________



=== Introdução
Já temos tratado uma aplicação da derivada no capítulo anterior, ao fazer o estudo das retas tangentes e normais ao gráfico de uma função.

Por outro lado, uma das aplicações mais importantes e úteis da derivada está no estudo de valores máximos e mínimos de uma função. Existem muitos problemas práticos nos quais trata-se de encontrar uma ``melhor'' maneira de formular-se problemas relacionados na determinação dos valores máximos e mínimos de uma função.

////
Quando pensamos na derivada como razão instantânea de uma função, representa-se muitas aplicações físicas da derivada, as aplicações mais obvias da derivada em problemas deste tipo, é a determinação da velocidade e aceleração de um objeto móvel os quais também serão estudados.
////




=== Valores Extremos  de uma Função

Já sabiamos que as funções contínuas sobre um intervalo fechado alcançam seu valor máximo e seu valor mínimo em pontos deste intervalo. Porém, até agora não disponhamos de um método para encontrá-os. Nesta seção resolveremos este problema. 

Definição:: Seja a função latexmath:[$f:{\rm dom}(f)\subset \mathbb{R}\to \mathbb{R}$]. Diz-se que:
...  latexmath:[$f$] tem um  *máximo relativo*, ou *máximo local*, em um ponto  latexmath:[$x^*\in {\rm dom}(f)$],  se existem um latexmath:[$\delta_1>0$], tal que 
+
[latexmath]
++++
\[
f(x^\circ)\geq f(x),\quad \forall\, x \in (w-\delta_1,w+\delta_1)\cap {\rm dom}(f).
\]
++++

...  latexmath:[$f$] tem um  *mínimo relativo*, ou *mínimo local*, em um ponto  latexmath:[$x_*\in {\rm dom}(f)$],  se existem um latexmath:[$\delta_2>0$], tal que 
+
[latexmath]
++++
\[
f(x_\circ)\leq f(x),\quad \forall\, x \in (w-\delta_2,w+\delta_2)\cap {\rm dom}(f).
\]
++++

...  latexmath:[$f$] tem um  *máximo absoluto*, ou *máximo global*, em um ponto  latexmath:[$x^\ast\in {\rm dom}(f)$],  se 
+
[latexmath]
++++
\[
f(x^\ast)\geq f(x),\quad \forall\, x \in {\rm dom}(f).
\]
++++

...  latexmath:[$f$] tem um  *mínimo absoluto*, ou *mínimo global*, em um ponto  latexmath:[$x_\ast\in {\rm dom}(f)$],  se 
+
[latexmath]
++++
\[
f(x_\ast)\leq f(x),\quad \forall\, x \in {\rm dom}(f).
\]
++++

Na seguinte figura podemos ver a ilustração destes conceitos:

image::images_cap_VI/valores_extremos.eps[scaledwidth="80%"] 
   
    

[NOTE]
====
* Todo máximo absoluto é um máximo relativo, de forma analoga, todo mínimo absoluto é um mínimo relativo;
* Os valores máximos e mínimos são chamados de *valores extremos* da função latexmath:[$f$];
* Uma função pode ter infinitos valores extremos;
* Uma função pode não ter um máximo ou um mínimo se o domínio for ilimitado ou não contiver um ponto extremo.
====

.{zwsp}
====
Dada a função latexmath:[$f(x)=x^3$] determinemos seus valores extremos, caso existam para os diferentes domínios: 
. latexmath:[${\rm dom}(f)=\mathbb{R}$]

Solução::

Não existem máximos nem mínimos, pois latexmath:[$f$] é ilimitada neste domínio. Veja o item (a) da figura abaixo.

. latexmath:[${\rm dom}(f)=[-2,2\]$]

Solução::

O mínimo absoluto de latexmath:[$-2$] é   latexmath:[$-8$] e o máximo absoluto de latexmath:[$2$] é   latexmath:[$8$]. Veja o item (b) da figura abaixo.

image::images_cap_VI/cubica.eps[scaledwidth="40%"] 

. latexmath:[${\rm dom}(f)=(-2,2\]$]

Solução::

Ausência de mínimo absoluto, pois  latexmath:[$-2 \notin {\rm dom}(f)$], e o máximo absoluto de latexmath:[$2$] é   latexmath:[$8$]. Veja item (c) da figura acima.

. latexmath:[${\rm dom}(f)=(-2,2)$].

Ausência de mínimo e máximo absoluto, pois  latexmath:[$-2, 2 \notin {\rm dom}(f)$]. Veja item (d) da figura acima.
====

[NOTE]
====
Algumas funções tem máximos e/ou mínimos  absolutos sobre um intervalo e outras não. O exemplo acima ilustra este fato.
====

Teorema(Teorema do Valor Extremo):: 
Se latexmath:[$f$] é uma função contínua em um intervalo fechado latexmath:[$[a,b\]$], então latexmath:[$f$] atinge tanto um valor máximo absoluto latexmath:[$M$] como  um valor mínimo absoluto latexmath:[$m$] neste intervalo. Isto é, existem números latexmath:[$x_1,\,\,\,x_2\in[a,b\]$] tais que 
[latexmath]
++++
\[
f(x_1)=m,\quad f(x_2)=M\quad \mbox{e}\quad m\leq f(x)\leq M\quad \mbox{para qualquer} \quad x \in [a,b].
\]
++++
Na seguinte figura ilustra localizações possíveis dos extremos absolutos de uma função contínua em um intervalo fechado latexmath:[$[a,b\]$]
// Figuras 4.3 de thommas

[NOTE]
====
Os requisitos do Teorema anterior de que o intervalos seja fechado e limitado, e a função seja contínua  são componentes fundamentais. sem eles, as conclusões do teorema não são válidas.
Se o intervalo não é fechado, o resultado deste teorema não necessariamente é verificado. Por exemplo,
a função latexmath:[$f(x)=\ln(x)$] é contínua  em latexmath:[$(0,1)$], porém, não tem nem mínimo nem máximo.
// figura

====


=== Determinando Valores Extremos  de uma Função

Teorema:: 
Seja latexmath:[$f: \mathbb{R} \to \mathbb{R}$] contínua e  latexmath:[$[a,b\]$] e derivável em latexmath:[$(a,b)$].
... Se latexmath:[$f'(x)>0$], para todo latexmath:[$x \in (a,b)$],  então latexmath:[$f$] é crescente em latexmath:[$[a,b\]$];

... Se latexmath:[$f'(x)<0$], para todo latexmath:[$x \in (a,b)$],  então latexmath:[$f$] é decrescente em latexmath:[$[a,b\]$].



Proposição (Condição suficiente com a derivada primeira)::
Seja latexmath:[$f$] uma função que esta definida em uma vizinhança latexmath:[$B(c,\delta)=(c-\delta,c+\delta)$] do ponto latexmath:[$c$] e é:
+
--
.. contínua em latexmath:[$B(c,\delta)$];
.. derivável em latexmath:[$B(c,\delta)$], exceto talvez em latexmath:[$c$].
--
+
Logo,
... Se latexmath:[$f'(x)>0$], para todo latexmath:[$x \in (c-\delta,c)$], e  latexmath:[$f'(x)<0$], para todo latexmath:[$x \in (c,c+\delta)$], então latexmath:[$f(c)$] é um valor máximo local de latexmath:[$f$];

... Se latexmath:[$f'(x)<0$], para todo latexmath:[$x \in (c-\delta,c)$], e  latexmath:[$f'(x)>0$], para todo latexmath:[$x \in (c,c+\delta)$], então latexmath:[$f(c)$] é um valor mínimo local de latexmath:[$f$].

Teorema (Teorema da derivada primeira para valores extremos locais):: 
Se latexmath:[$f$] possui um valor máximo ou um mínimo local em um ponto latexmath:[$c$] interior de seu domínio e se latexmath:[$f'$] é definida em latexmath:[$c$], então
[latexmath]
++++
\[
f'(c)=0.
\]
++++

Este teorema nos diz que a primeira derivada  de uma função é sempre zero em um ponto interior em que a  função tenha um valor extremo local  e a derivada seja definida. Assim, os únicos locais em que uma função latexmath:[$f$] pode ter valores extremos (locais ou globais) são

. pontos interiores em que  latexmath:[$f'=0$];
. pontos interiores em que  latexmath:[$f'$] não existe;
. extremidades do domínio de latexmath:[$f$].


Proposição (Condição suficiente com a derivada segunda):: 
Seja latexmath:[$f$] uma função tal que:
+
--
.. Tem derivadas até segundo ordem, contínuas em uma vizinhança latexmath:[$B(c,\delta)$] do ponto latexmath:[$c$];
.. latexmath:[$f'(c)=0$];
.. latexmath:[$f''(c)\neq 0$].
--
+
Logo,
... Se latexmath:[$f'(c)<0$], então latexmath:[$f(c)$] é um valor máximo local de latexmath:[$f$];
... Se latexmath:[$f'(c)>0$], então latexmath:[$f(c)$] é um valor mínimo local de latexmath:[$f$].



A definição apresentada a seguir resume estas informações.

Definição:: um ponto interior do domínio de uma função latexmath:[$f$] em que latexmath:[$f'$] é zero ou indefinida é um *ponto crítico* de latexmath:[$f$].  


[NOTE]
====
. Critério da derivada primeira para encontrar extremos

A segunda proposição nos permite estabelecer o seguinte critério para determinar os valores máximos e/ou mínimos relativos de uma função contínua

. Determinar os pontos críticos da função latexmath:[$f$];

. Se latexmath:[$c$] é um ponto crítico, devemos determinar o sinal  de latexmath:[$f'(x)$], primeiro para latexmath:[$x<c$], suficientemente próximos, e depois para latexmath:[$c<x$], suficientemente próximos.

... Se o sinal muda de latexmath:[$-$] para latexmath:[$+$], latexmath:[$f(c)$] é um valor mínimo relativo;
... Se o sinal muda de latexmath:[$+$] para latexmath:[$-$], latexmath:[$f(c)$] é um valor máximo relativo;
... Se não existe variação do sinal, não existem nem máximos nem mínimos relativos a latexmath:[$c$].

====

[NOTE]
====
. Critério da derivada segunda para encontrar extremos

A terceira proposição nos permite estabelecer o seguinte critério para determinar os valores máximos e/ou mínimos relativos de uma função contínua

. Determinar os pontos críticos da função latexmath:[$f$];

. Se latexmath:[$c$] é um ponto crítico, calcular latexmath:[$f''(c)$].

... Se latexmath:[$f''(c)>0$], latexmath:[$f(c)$] é um valor mínimo relativo;
... Se latexmath:[$f''(c)<0$], latexmath:[$f(c)$] é um valor máximo relativo;
... Se latexmath:[$f''(c)=0$] ou latexmath:[$f''(c)$] não existe, este critério é inconsistente.

====

.{zwsp}
====
Determinemos os intervalos de crescimento e decrescimento, e os valores extremos de latexmath:[$f$]:

. latexmath:[$f(x)=2x^3+6x^2-48x+9$].
Solução::
.. latexmath:[${\rm dom}(f)=\mathbb{R}$];
.. Para determinar os intervalos de crescimento e decrescimento, da *Proposição 1*, é suficiente determinar os intervalos onde latexmath:[$f'$]  é positiva ou negativa;
.. latexmath:[$f'(x)=6x^2+12x-48=6(x+4)(x-2)$], logo os pontos críticos são:
+
[latexmath]
++++
\[
x=-4\quad \mbox{e}\quad x=2.
\]
++++
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f'$] | Cresc. ou Decresc.
| latexmath:[$(-\infty,-4)$] | latexmath:[$+$] | cresce
| latexmath:[$(-4,2)$]       | latexmath:[$-$] | decresce
| latexmath:[$(2,+\infty)$]  | latexmath:[$+$] | cresce
|======================
+
Agora, analisando os valores extremos temos que:

* latexmath:[$f(-4)=169$] é um  máximo relativo
* latexmath:[$f(2)=-47$] é um mínimo relativo

. latexmath:[$f(x)=\dfrac{4}{x-1}+\dfrac{x+2}{2}$].
Solução::
.. latexmath:[${\rm dom}(f)=\mathbb{R}\setminus\{1\}$];
.. Para determinar os intervalos de crescimento e decrescimento, da *Proposição 1*, é suficiente determinar os intervalos onde latexmath:[$f'$]  é positiva ou negativa;
.. latexmath:[$f'(x)=-\dfrac{4}{(x-1)^2}+ \dfrac{1}{3}=\dfrac{(x+1)(x-3)}{3(x-1)^2}$], logo os pontos críticos são:
+
[latexmath]
++++
\[
x=-1,\quad x=1\quad\mbox{e}\quad x=3.
\]
++++
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f'$] | Cresc. ou Decresc.
| latexmath:[$(-\infty,-1)$] |  latexmath:[$+$] | cresce
| latexmath:[$(-1,1)$] |  latexmath:[$-$] | decresce
| latexmath:[$(1,3)$] |  latexmath:[$-$] | decresce
| latexmath:[$(3,+\infty)$] |  latexmath:[$+$] | cresce 
|======================
+
Agora, analisando os valores extremos temos que:

* latexmath:[$f(-1)=- \dfrac{5}{3} $] é um  mínimo relativo;
* latexmath:[$f(1)$] não existe, logo latexmath:[$x=1$] é uma assintota;
* latexmath:[$f(3)= \dfrac{11}{3} $] é um máximo relativo.


. latexmath:[$f(x)=3x^{1/3}(x+4)^{2/3}$].

Solução::

.. latexmath:[${\rm dom}(f)=\mathbb{R}$];
.. Para determinar os intervalos de crescimento e decrescimento, da *Proposição 1*, é suficiente determinar os intervalos onde latexmath:[$f'$]  é positiva ou negativa;
.. latexmath:[$f'(x)=\dfrac{(x+4)^{2/3}}{x^{2/3}}+ \dfrac{2x^{1/3}}{(x+4)^{1/3}}=\dfrac{3x+4}{x^{2/3}(x+4)^{1/3}}$], logo os pontos críticos são:
+
[latexmath]
++++
\[
x=-4,\quad x=-\dfrac{4}{3}\quad\mbox{e}\quad x=0.
\]
++++
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f'$] | Cresc. ou Decresc.
|latexmath:[$(-\infty,-4)$] |  latexmath:[$+$] | cresce
|latexmath:[$\left(-4,-\dfrac{4}{3}\right)$] |  latexmath:[$-$] | decresce
|latexmath:[$\left(-\dfrac{4}{3},0\right)$] |  latexmath:[$+$] | cresce
|latexmath:[$(0,+\infty)$] |  latexmath:[$+$] | cresce
|======================
+
Agora, analisando os valores extremos temos que:

* latexmath:[$f(-4)=0 $] é um  máximo relativo;
* latexmath:[$f\left(-\dfrac{4}{3}\right)=-2^{8/3}$] é um  mínimo relativo;
* latexmath:[$f(0)=$]  não existe, logo latexmath:[$x=0$] é uma assintota.

. latexmath:[$f(x)=\left\{\begin{array}{ll}
	\sqrt{16-(x+3)^2},& \mbox{se }x\leq 1\\
	1-x^2,& \mbox{se }x> 1\\
\end{array}\right.$].
Solução::

.. latexmath:[${\rm dom}(f)=[-7,+\infty)$];
.. Para determinar os intervalos de crescimento e decrescimento, da *Proposição 1*, é suficiente determinar os intervalos onde latexmath:[$f'$]  é positiva ou negativa;
.. latexmath:[$f'(x)=\left\{\begin{array}{ll}
	-\dfrac{x+3}{\sqrt{16-(x+3)^2}},& \mbox{se }x< 1\\
	-2x,& \mbox{se }x> 1\\
\end{array}\right.$], logo os pontos críticos são:
+
[latexmath]
++++
\[
 x=-3,\quad \mbox{e}\quad x=1.
\]
++++
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f'$] | Cresc. ou Decresc.
| latexmath:[$(-7,-3)$] |  latexmath:[$+$] | cresce
| latexmath:[$(-3,1)$] |  latexmath:[$-$] | decresce
|======================
+
Agora, analisando os valores extremos temos que:

* latexmath:[$f(-3)=4 $] é um  máximo relativo.

====

=== Teorema do Valor Médio

Sabemos que funções constantes têm derivadas iguais a zero, mas poderia existir uma função mais complicada cujas derivadas fossem sempre zero?.....constante   

Teorema (Teorema de Rolle)::
Seja latexmath:[$f$] contínua em latexmath:[$[a,b\]$], latexmath:[$a<b$], e derivável em latexmath:[$(a,b)$]
 tal que latexmath:[$f(a)=0$] e latexmath:[$f(b)=0$], então existe pelo menos um ponto latexmath:[$c\in (a,b)$] que verifica
[latexmath]
++++
\[
f'(c)=0.
\]
++++
%figura  513 venero

[NOTE]
====
. Neste teorema, a condição de continuidade de latexmath:[$f$] em latexmath:[$[a,b\]$] é obviamente muito importante, pois garante que o gráfico não  de latexmath:[$f$] não tenha saltos bruscos  dentro de latexmath:[$[a,b\]$].
. Este teorema segue sendo válido se a hipótese (c) é substituída por latexmath:[$f(a)=f(b)$].
====

//figura 6.7 mitacc 249

Teorema do Valor Médio (Teorema de Lagrange):: Seja latexmath:[$f:[a,b\]\to \mathbb{R}$] uma função tal que 
+
--
... latexmath:[$f$] é contínua em latexmath:[$[a,b\]$];
... latexmath:[$f$] é diferenciável em latexmath:[$(a,b)$].
--
+
Então, existe pelo menos um latexmath:[$c \in(a,b) $] tal que
[latexmath]
++++
\[
f'(c)=\dfrac{f(b)-f(a)}{b-a},
\]
++++
ou equivalentemente, 
[latexmath]
++++
\[
f'(c)(b-a)=f(b)-f(a).
\]
++++
//% figura mitacc 6.8 pag. 250

Teorema Generalizado do valor médio de Cauchy:: Sejam latexmath:[$f$] e latexmath:[$g:[a,b\]\to \mathbb{R}$] uma função tal que 
+
--
... latexmath:[$f$] e latexmath:[$g$] são contínuas em latexmath:[$[a,b\]$];
... latexmath:[$f$]latexmath:[$f$] são diferenciáveis em latexmath:[$(a,b)$];
... latexmath:[$g(a)\neq g(b)$] e latexmath:[$g'(x)\neq 0$], latexmath:[$\forall x \in (a,b)$].
--
+
Então, existe pelo menos um latexmath:[$c \in(a,b) $] tal que
[latexmath]
++++
\[
\dfrac{f'(c)}{g'(c)}=\dfrac{f(b)-f(a)}{g(b)-g(a)}.
\]
++++

Teorema(da Função constante)::
Se latexmath:[$f'(x)=0$], para todo latexmath:[$x \in (a,b)$], então latexmath:[$f$] é uma função constante 
em latexmath:[$ (a,b)$].

Teorema(da diferença constante):: Sejam latexmath:[$f$] e latexmath:[$g$] funções contínuas em latexmath:[$[a,b\]$]. 
Então, latexmath:[$f'(x)=g'(x)$] para todo latexmath:[$x \in (a,b)$] se, e somente se, latexmath:[$
f(x)=g(x)+c$], onde latexmath:[$c$] é uma constante.

//EXEMPLOS



=== Concavidade e esboço de curvas

O conceito de concavidade é muito útil no esboço do gráfico de uma curva.
Na figura abaixo, observamos que dado um ponto qualquer latexmath:[$c$] entre latexmath:[$a$] e latexmath:[$
b$], em pontos próximos de latexmath:[$c$] o gráfico de latexmath:[$f$] estará acima da tangente à curva no ponto latexmath:[$P (c, f(c))$]. Dizemos que a curva tem concavidade voltada para cima no intervalo latexmath:[$(a,b)$].

Como latexmath:[$f'(x)$] é a inclinação da reta tangente à curva, observa-se  na figura abaixo que no intervalo latexmath:[$(a,b)$] a derivada latexmath:[$f'(x)$] é crescente.

Geometricamente falando isso significa que a reta tangente gira no sentido anti-horário à medida que avançamos sobre a curva da esquerda para a direita.

Analogamente, o mesmo vale para uma função que tem concavidade voltada para baixo no intervalo latexmath:[$(a,b)$].

 

Na figura b, vemos que a tangente gira no sentido horário quando nos deslocamos sobre a curva da esquerda para a direita. A derivada latexmath:[$f'(x)$] é decrescente em latexmath:[$(a,b)$].

Assim temos as seguintes definições:

Definição 1:: Uma função latexmath:[$f$] é dita *côncava para cima* no intervalo latexmath:[$(a,b)$], se latexmath:[$f'(x)$] é crescente nesse intervalo.

// %figura 4.25 thomas  pag. 233 ou 7, 24-25 mitacc 290

Definição 2:: Uma função latexmath:[$f$] é côncava para baixo no intervalo latexmath:[$(a,b)$], se latexmath:[$f'(x)$] for decrescente neste intervalo.

//%figura


[NOTE]
====
Tendo em conta que a reta tangente ao gráfico de latexmath:[$f$], no ponto latexmath:[$P(c,f(c))$] divide o  plano em dois semi-planos (um superior e outro inferior). Logo, dizer que a curva é concava para  cima no ponto latexmath:[$c$] significa que seu gráfico encontra-se no semi-plano superior,   ou que a reta tangente  se encontra por cima da curva.

De forma análoga, dizer que a curva é concava para  baixo no ponto latexmath:[$c$] significa que seu gráfico encontra-se no semi-plano inferior,   ou que a reta tangente  se encontra por baixo da curva.
====

Definição (Ponto de inflexão):: Seja latexmath:[$f$] uma função contínua no ponto  latexmath:[$c$]. Diz-se que latexmath:[$P(c,f(c))$] é um *ponto de inflexão* de latexmath:[$f$], se existe um latexmath:[$\delta>0$] tal que  as concavidades nos intervalos latexmath:[$(c-\delta,c)$] e latexmath:[$(c, c+\delta)$] são diferentes. Em outras palavras, um *ponto de inflexão* é um ponto em que o gráfico de uma função possui uma reta tangente e onde há mudança de concavidade.

// %figura 7.26  mitacc ou  4.26 thomas

Proposição 4 (Teste da segunda derivada para concavidade):: Seja  latexmath:[$f: \mathbb{R}\to \mathbb{R}$] derivável  em uma vizinhança  latexmath:[$B(c,\delta)\subset {\rm dom}(f)$] e latexmath:[$f''(c)\neq 0$].
... Se latexmath:[$f''(c)> 0$], então latexmath:[$f$] é concava para cima no ponto latexmath:[$c$];
... Se latexmath:[$f''(c)< 0$], então latexmath:[$f$] é concava para baixo no ponto latexmath:[$c$].

Corolário:: Se latexmath:[$f$] é derivável duas vezes em latexmath:[$B(c,\delta)\subset {\rm dom}(f)$] e latexmath:[$P(c,f(c))$] é um ponto de inflexão de latexmath:[$f$], então latexmath:[$f''(c)= 0$].

Proposição 5 (Condição suficiente para pontos de inflexão):: Seja latexmath:[$f$]  duas vezes derivável em latexmath:[$B(c,\delta)\subset {\rm dom}(f)$], exceto talvez em latexmath:[$x=c$], porém contínua em latexmath:[$x=c$] e 
+
--
... latexmath:[$f''(c)=0$] ou não existe  latexmath:[$f''(c)$];
... latexmath:[$f''$] tem sinais opostos  em latexmath:[$(c-\delta,c)$] e em latexmath:[$(c, c+\delta)$].
--
+
Então, latexmath:[$P(c,f(c))$] é um ponto de inflexão.

Corolário:: Seja latexmath:[$f$]  duas vezes derivável em latexmath:[$B(c,\delta)\subset {\rm dom}(f)$] tal que latexmath:[$f''(c)=0$] e latexmath:[$f'''(c)\neq0$]. Então, latexmath:[$P(c,f(c))$] é um ponto de inflexão.

[NOTE]
====
.Critério para determinar os pontos de inflexão de uma função

Das proposições 4 e 5 podemos estabelecer o seguinte critério para encontrar os pontos de inflexão de uma função contínua latexmath:[$f$]:
. Encontrar os valores de latexmath:[$x$] para os quais latexmath:[$f''(x)=0$] ou latexmath:[$f''(x)$] não existe. A estes valores os chamaremos de *pontos críticos de inflexão*;
. Determinar o sinal de latexmath:[$f''(x)$] para valores menores e maiores, suficientemente próximos, a cada ponto crítico de inflexão:
.. Se latexmath:[$f''(x)$] muda de sinal, então o *pci* avaliado é um ponto de inflexão;
.. Se latexmath:[$f''(x)$] permanece com o mesmo sinal, então o *pci* avaliado não é um ponto de inflexão.

====



.{zwsp}
====
Analisemos a concavidade e encontremos os pontos de inflexão de latexmath:[$f$] definida por:

. latexmath:[$f(x)=x^6-x^5$]

Solução::
.. latexmath:[$f$] é contínua em latexmath:[$\mathbb{R}$];
.. latexmath:[$f'(x)=6x^5-5x^4$];
.. latexmath:[$f''(x)=30x^4-20x^3=10x^3(3x-2)$];
+
Logo, os pontos críticos de inflexão são:
+
[latexmath]
++++
\[
x=0\quad \mbox{e} \quad x=2/3.
\]
++++
+
O analises dos sinais de latexmath:[$f''(x)$] são mostrados na tabela a seguir:
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f''$] | Concavidade
|latexmath:[$(-\infty, 0)$] | latexmath:[$+$] | para cima
|latexmath:[$(0,2/3)$]| latexmath:[$-$]| para baixo
|latexmath:[$(2/3,+\infty)$]| latexmath:[$+$]| para cima
|======================
+
Portanto, latexmath:[$P(0,0)$] e latexmath:[$P(\dfrac{2}{3},-\dfrac{32}{729})$] são pontos de inflexão. 

. latexmath:[$f(x)=\left\{\begin{array}{ll}
	(x-3)^2& \mbox{se } x\geq 3;\\
	-\sqrt[3\]{x-3}& \mbox{se } x< 3
\end{array}\right.$]

Solução::
.. latexmath:[$f$] é contínua em latexmath:[$\mathbb{R}$];
.. latexmath:[$f'(x)=\left\{\begin{array}{ll}
	2(x-3)& \mbox{se } x> 3;\\
	-\dfrac{1}{\sqrt[3\]{3(x-3)^2}}& \mbox{se } x< 3
\end{array}\right.$]
.. latexmath:[$f''(x)=\left\{\begin{array}{ll}
	2& \mbox{se } x> 3;\\
	\dfrac{2}{\sqrt[3\]{9(x-3)^5}}& \mbox{se } x< 3
\end{array}\right.$]
+
Logo, o pontos críticos de inflexão é latexmath:[$x=3$], pois latexmath:[$f''(3)$] não existe.
+
O analises dos sinais de latexmath:[$f''(x)$] são mostrados na tabela a seguir:
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f''$] | Concavidade
|latexmath:[$(-\infty, 3)$]| latexmath:[$-$]| para cima
|latexmath:[$(3,+\infty)$]| latexmath:[$+$]| para cima
|======================
+
Portanto, latexmath:[$P(3,0)$] é  ponto de inflexão.

. latexmath:[$f(x)=\dfrac{x+1}{x-5}$]

Solução::
.. latexmath:[$f$] é contínua em latexmath:[$\mathbb{R}\setminus \{5\}$];
.. latexmath:[$f'(x)=- \dfrac{6}{(x-5)^2}$];
.. latexmath:[$f''(x)=\dfrac{12}{(x-5)^3}$];
+
Embora que para latexmath:[$ x=5$] latexmath:[$f''(5)$] não exista, ele não é um ponto críticos de inflexão  porque latexmath:[$ 5 \notin {\rm dom}(f)$]. Portanto, latexmath:[$f$] não tem pontos de inflexão. 
+
O analises dos sinais de latexmath:[$f''(x)$] são mostrados na tabela a seguir:
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f''$] | Concavidade
|latexmath:[$(-\infty, 5)$]| latexmath:[$-$]| para baixo
|latexmath:[$(5,+\infty)$]| latexmath:[$+$]| para cima
|======================
//% figuara 7.27 mitacc  mitacc

====


Proposição 6 (Condição suficiente de concavidade e pontos de inflexão com a latexmath:[$n-$]ésima derivada)::
Se latexmath:[$f:\mathbb{R}\to \mathbb{R}$], satisfaz as seguintes condições:
+
--
.. Derivadas contínuas até a ordem latexmath:[$n$] em uma vizinhança latexmath:[$B(c,\delta)\subset {\rm dom}(f)$];
.. latexmath:[$f''(c)=f'''(c)=\dot=f^{(n-1)}(c)=0$];
.. latexmath:[$f^{(n)}(c)\neq 0$].
--
+
Então,
+
--
... Se latexmath:[$n$] é par e latexmath:[$f^{(n)}(c)>0$], então  latexmath:[$f$] é concava para cima em latexmath:[$x=c$];
... Se latexmath:[$n$] é par e latexmath:[$f^{(n)}(c)<0$], então  latexmath:[$f$] é concava para baixo em latexmath:[$x=c$];
... Se latexmath:[$n$] é impar, então  latexmath:[$c$] é um ponto de inflexão.
--

Proposição 7 (Condição suficiente de extremo e pontos de inflexão com a latexmath:[$n-$]ésima derivada)::
Se latexmath:[$f:\mathbb{R}\to \mathbb{R}$], satisfaz as seguintes condições:
+
--
.. Derivadas contínuas até a ordem latexmath:[$n$] em uma vizinhança latexmath:[$B(c,\delta)\subset {\rm dom}(f)$];
.. latexmath:[$f''(c)=f'''(c)=\dot=f^{(n-1)}(c)=0$];
.. latexmath:[$f^{(n)}(c)\neq 0$].
--
+
Então,
+
--
... Se latexmath:[$n$] é par e latexmath:[$f^{(n)}(c)>0$], então  latexmath:[$c$] é um ponto de mínimo de  latexmath:[$f$];
... Se latexmath:[$n$] é par e latexmath:[$f^{(n)}(c)<0$],  então  latexmath:[$c$] é um ponto de máximo de  latexmath:[$f$];
... Se latexmath:[$n$] é impar, então  latexmath:[$c$] é um ponto de inflexão.
--

.{zwsp}
====
Determinemos os valores extremos de latexmath:[$f$] definida por:

. latexmath:[$f(x)=(x-2)^6$]

Solução::

Da definição de latexmath:[$f$] temos que 
+
[latexmath]
++++
\[
f'(x)=6(x-2)^5,\quad f''(x)=30(x-2)^4,\quad f'''(x)=120(x-2)^3
\]
\[
f^{(4)}(x)=360(x-2)^2,\quad f^{(5)}(x)=720(x-2),\quad f^{(6)}(x)=720
\]
++++
+
Logo, a equação latexmath:[$f'(x)=0$] admite a única solução em latexmath:[$x=2$]. +
Como latexmath:[$f'(2)=f''(2)=f'''(2)=f^{(4)}(2)=f^{(5)}(2)=0$], latexmath:[$f^{(5)}(2)>0$] e latexmath:[$n=6$] é par, então existe um mínimo em latexmath:[$x=2$], e com valor mínimo  latexmath:[$f(2)=0$].

. latexmath:[$f(x)=(x-2)^5+5$]

Solução::

Da definição de latexmath:[$f$] temos que 
+
[latexmath]
++++
\[
f'(x)=5(x-2)^4,\quad f''(x)=20(x-2)^3,\quad f'''(x)=60(x-2)^2
\]
\[
f^{(4)}(x)=120(x-2),\quad f^{(5)}(x)=120
\]
++++
+
Logo, a equação latexmath:[$f'(x)=0$] admite a única solução em latexmath:[$x=2$]. +
Como latexmath:[$f'(2)=f''(2)=f'''(2)=f^{(4)}(2)=0$], latexmath:[$f^{(5)}(2)>0$] e latexmath:[$n=5$] é impar, então existe um ponto de inflexão em latexmath:[$x=2$].
====


=== Construindo o gráfico de latexmath:[$y=f(x)$]

A construção do gráfico de uma função é muito importante, pois com ela podemos determinar o comportamento da função. Construi-se o gráfico com a ajuda dos limites e das derivadas. O procedimento é o seguinte:

. Determinar o domínio de latexmath:[$f$], latexmath:[${\rm dom}(f)$];
. Determinar as interseções com os eixos;
. Verificar a simetria da função, a existência de assintotas, e calcular limites nos extremos de  latexmath:[${\rm dom}(f)$] e nos pontos de descontinuidade, a fim de determinar o comportamento da função nesses pontos;
. Determinar os intervalos de crescimento e os valores extremos da função;
. Determinar os intervalos de concavidade e os pontos de inflexão;
. Construir o gráfica da função, com a informação obtida.


.{zwsp}
====
Construa o gráfico de latexmath:[$f$] definida por:

. latexmath:[$f(x)=\dfrac{x^2-x-2}{x-5}$]

Solução:: 
+
--
.. latexmath:[${\rm dom}(f)=\mathbb{R}\setminus\{5\}$];
.. Interseções com os eixos: 
+
--
* Com o eixo latexmath:[$y$]: para  latexmath:[$x=0$],  temos que  latexmath:[$f(0)=2/5$] 
* Com o eixo latexmath:[$x$]: para latexmath:[$f(x)=0$], temos que latexmath:[$x=-1$] ou  latexmath:[$x=2$]
--
+
Logo, os ponto de interseção com os eixos são: latexmath:[$(0,2/5)$], latexmath:[$(-1,0)$] e latexmath:[$
(2,0)$].

.. O gráfico não é simétrico com respeito ao eixo latexmath:[$y$] pois latexmath:[$f(-x)\neq f(x)$]
+
--
* Assintota vertical: latexmath:[$x=5$] porque
+
[latexmath]
++++
\[
\lim\limits_{x\to 5^-}f(x)=-\infty\quad \mbox{e}\quad \lim\limits_{x\to 5^+}f(x)=+\infty;
\]
++++
* Assintotas horizontais: não existem porque
+
[latexmath]
++++
\[
\lim\limits_{x\to-\infty}f(x)=-\infty\quad \mbox{e}\quad \lim\limits_{x\to+\infty}f(x)=+\infty;
\]
++++
* Assintotas oblíquas: latexmath:[$y=x+4$] é a única oblíqua porque
+
[latexmath]
++++
\[
\lim\limits_{x\to \mp\infty}\dfrac{f(x)}{x}=1\quad \mbox{e}\quad \lim\limits_{x\to\pm\infty}(f(x)-x)=4.
\]
++++
--

.. Como latexmath:[$f'(x)= \dfrac{x^2-10x+7}{(x-5)^2}$], logo os pontos críticos são latexmath:[$x=5-3\sqrt{2}$] e  latexmath:[$x=5+3\sqrt{2}$]. O analise dos sinais de latexmath:[$f'(x)$] é mostrado na tabela a seguir:
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f'$] | Cresc. ou Decresc.
|latexmath:[$(-\infty,5-3\sqrt{2} )$] |  latexmath:[$+$] |  crescente
|latexmath:[$(5-3\sqrt{2},5)$] |  latexmath:[$-$] |  decrescente
|latexmath:[$(5,5+3\sqrt{2})$] |  latexmath:[$-$] |  decrescente
|latexmath:[$(5+3\sqrt{2}, +\infty )$] |  latexmath:[$+$]| crescente
|======================
+
Então, latexmath:[$f(5-3\sqrt{2})$] é um valor máximo e latexmath:[$f(5+3\sqrt{2})$] é um valor mínimo.

.. Como latexmath:[$f''(x)= \dfrac{36}{(x-5)^3}$], logo não existem pontos críticos de inflexão, pois latexmath:[$5 \notin {\rm dom}(f)$]. O analise dos sinais de latexmath:[$f''(x)$] é mostrado na tabela a seguir:
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f''$] |  Concavidade
|latexmath:[$(-\infty,5 )$]| latexmath:[$-$]|para baixo
|latexmath:[$(5, +\infty )$]| latexmath:[$+$]| para cima
|======================
--
+
Portanto, o gráfico de latexmath:[$f$] é:

// figura 7.32 mitacc pag. 300

. latexmath:[$f(x)=\dfrac{x}{x^2-4}$]

Solução::
+
--
.. latexmath:[${\rm dom}(f)=\mathbb{R}\setminus \{-2,2\}$];
.. Interseções com os eixos: latexmath:[$(0,0)$];
.. O gráfico de latexmath:[$f$] não é simétrico com respeito ao eixo latexmath:[$y$], porque latexmath:[$f(-x)\neq f(x)$].
+
--
* Assintotas verticais: latexmath:[$x=-2$]  e latexmath:[$x=2$],  porque
+
[latexmath]
++++
\[
\lim\limits_{x\to -2^-}f(x)=-\infty\quad \mbox{e}\quad \lim\limits_{x\to -2^+}f(x)=+\infty;
\]
\[
\lim\limits_{x\to 2^-}f(x)=-\infty\quad \mbox{e}\quad \lim\limits_{x\to 2^+}f(x)=+\infty;
\]
++++
* Assintota horizontal: latexmath:[$y=0$];
* Assintotas oblíquas: não existem.
--
+
.. Como latexmath:[$f'(x)= -\dfrac{x^2+4}{(x^2-4)^2}$], não existem pontos críticos e  latexmath:[$f'(x)<0$], para todo latexmath:[$x \in {\rm dom}(f)$]. Logo,  latexmath:[$f$] é decrescente em latexmath:[$
(-\infty, -2)$], latexmath:[$(-2,2)$] e latexmath:[$(2, +\infty)$].

.. Como latexmath:[$f''(x)= \dfrac{2(x^2+12)}{(x^2-4)^3}$], logo latexmath:[$x=0$] é um ponto crítico de inflexão. O analise dos sinais de latexmath:[$f''(x)$] é mostrado na tabela a seguir:
+
[width="70%",cols="1,1,1",frame="topbot",options="header"]
|======================
| Intervalos | Sinal de latexmath:[$f''$] | Concavidade
|latexmath:[$(-\infty,-2 )$]| latexmath:[$-$]|para baixo
|latexmath:[$(-2,0)$]|latexmath:[$+$]| para cima
|latexmath:[$(-2,0)$]|latexmath:[$-$]|para baixo
|latexmath:[$(2, +\infty )$]| latexmath:[$+$]| para cima
|======================
+
Logo, o ponto latexmath:[$P(0,0)$] é um ponto de inflexão;
--
+
Portanto, o gráfico de latexmath:[$f$] é:

// %figura 7.33 mitacc pag. 300

====


=== Formas indeterminadas e regra de  L'Hôpital
Em muitos exemplos das seções anteriores temos calculado o limite de um quociente latexmath:[$f(x)/g(x)$] no que o numerador latexmath:[$f(x)$] e o denominador latexmath:[$g(x)$] tendem a latexmath:[$0$], latexmath:[$\infty$]. Os exemplos deste tipo se diz que o quociente latexmath:[$f(x)/g(x)$] adota uma forma indeterminada.

Uma forma de resolver estes problemas é obtendo polinômios de aproximação para latexmath:[$f(x)$] e para latexmath:[$g(x)$]. Algumas vezes o trabalho pode ser abreviado com o uso de uma técnica  de derivação denominada *regra de  L'Hôpital*



Teorema (Forma indeterminada latexmath:[$0/0$]):: Suponha que latexmath:[$f(a)=g(a)=0$], que  latexmath:[$f$] e latexmath:[$g$] sejam deriváveis  em um intervalo aberto latexmath:[$I$], com latexmath:[$a \in I$] e que latexmath:[$g'(x)\neq 0$] em latexmath:[$I$]. Então,
[latexmath]
++++
\[
\lim \limits_{x\to a}\dfrac{f(x)}{g(x)}=\lim \limits_{x\to a}\dfrac{f'(x)}{g'(x)},
\]
++++
supondo que o limite do lado direito dessa equação exista.

[NOTE]
====
. Em alguns casos pode ser necessário aplicar a *regra de L'Hôpital* repetidas vezes
. Se latexmath:[$a=\infty$], podemos fazer latexmath:[$x=\dfrac{1}{z}$], e o problema se reduz a:
[latexmath]
++++
\[
\lim \limits_{x\to \infty}\dfrac{f(x)}{g(x)}=\lim \limits_{z\to 0}\dfrac{-f'\left( \dfrac{1}{z}\right)\dfrac{1}{z^2}}{-g'\left( \dfrac{1}{z}\right)\dfrac{1}{z^2}}=\lim \limits_{z\to 0}\dfrac{f'\left( \dfrac{1}{z}\right)}{g'\left( \dfrac{1}{z}\right)}=\lim \limits_{x\to \infty}\dfrac{f'(x)}{g'(x)}.
\]
++++
====


.{zwsp}
====
Calcular os seguintes limites:

. latexmath:[$\lim \limits_{x\to 0}\dfrac{1-e^{2x}}{x}$]

Solução::

Fazendo latexmath:[$f(x)=1-e^{2x}$] e latexmath:[$g(x)=x$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}f(x)=0 \quad \mbox{e}\quad \lim \limits_{x\to 0}g(x)=0
\]
++++
+
Logo, podemos usar a regra de L'hôpital para. Das definições de latexmath:[$f(x)$] e latexmath:[$g(x)$], temos que latexmath:[$f'(x)=-2e^{2x}$] e latexmath:[$g'(x)=1$]. Assim
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}\dfrac{1-e^{2x}}{x}=\lim \limits_{x\to 0}\dfrac{-2e^{2x}}{1}=-2.
\]
++++

. latexmath:[$\lim \limits_{x\to 0}\dfrac{{\rm sen}(x)}{x}$]

Solução::

Fazendo latexmath:[$f(x)={\rm sen}(x)$] e latexmath:[$g(x)=x$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}f(x)=0 \quad \mbox{e}\quad \lim \limits_{x\to 0}g(x)=0
\]
++++
+
Logo, podemos usar a regra de L'Hôpital para. Das definições de latexmath:[$f(x)$] e latexmath:[$g(x)$], temos que latexmath:[$f'(x)=\cos(x)$] e latexmath:[$g'(x)=1$]. Assim
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}\dfrac{1-e^{2x}}{x}=\lim \limits_{x\to 0}\dfrac{\cos(x)}{1}=1.
\]
++++

. latexmath:[$\lim \limits_{x\to 0}\dfrac{x-{\rm tan}(x)}{x-{\rm sen}(x)}$]

Solução::

Fazendo latexmath:[$f(x)=x-{\rm tan}(x)$] e latexmath:[$g(x)=x-{\rm sen}(x)$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}f(x)=0 \quad \mbox{e}\quad \lim \limits_{x\to 0}g(x)=0
\]
++++
+
Logo, podemos usar a regra de L'Hôpital. Das definições de latexmath:[$f(x)$] e latexmath:[$g(x)$], temos que latexmath:[$f'(x)=1-{\rm sec}^2(x)$] e latexmath:[$g'(x)=1-\cos(x)$]. Porém, 
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}f'(x)=0 \quad \mbox{e}\quad \lim \limits_{x\to 0}g'(x)=0
\]
++++
+
Desde que o quociente das derivadas latexmath:[$\dfrac{f'(x)}{g'(x)}$] tende a forma indeterminada latexmath:[$0/0$], Da *Nota* anterior, podemos aplicar a regra de L'Hôpital repetidas vezes até eliminar esta indeterminação. Neste caso, aplicaremos duas vezes mais a regra de L'Hôpital, ou seja, até a derivada de ordem latexmath:[$3$]. Assim
+
[latexmath]
++++
\[
\lim \limits_{x\to 0}\dfrac{x-{\rm tan}(x)}{x-{\rm sen}(x)}=\lim \limits_{x\to 0}\dfrac{1-{\rm sec}^2(x)}{1-\cos(x)}=\lim \limits_{x\to 0}\dfrac{-2{\rm tan}(x){\rm sec}^2(x)}{{\rm sen}(x)}=\lim \limits_{x\to 0}\dfrac{-2(1+3{\rm tan}^2(x))}{\cos^2(x)}=-2.
\]
++++
====


Formas Indeterminadas latexmath:[$\infty/\infty$], latexmath:[$\infty\cdot0$], latexmath:[$\infty-\infty$]::
 
Às vezes quando tentamos avaliar um limite quando latexmath:[$x\to a$], pela substituição de latexmath:[$x=a$], obtemos uma forma indeterminada como latexmath:[$\infty/\infty$], latexmath:[$\infty\cdot0$] ou latexmath:[$\infty-\infty$], em vez de latexmath:[$0/0$]. 
 
*Forma Indeterminada latexmath:[$\infty/\infty$]:* Em tratamentos mais avançados de cálculo é provado que  a *regra de L'Hôpital* aplica-se à forma indeterminada  latexmath:[$\infty/\infty$], da mesma forma que latexmath:[$0/0$], ou seja, se latexmath:[$f(x)\to \pm\infty$] e latexmath:[$g(x)\to \pm\infty$], quando latexmath:[$x\to a$], então
[latexmath]
++++
\[
\lim \limits_{x\to a}\dfrac{f(x)}{g(x)}=\lim \limits_{x\to a}\dfrac{\frac{1}{g(x)}}{\frac{1}{f(x)}}=\lim \limits_{x\to a}\dfrac{f'(x)}{g'(x)}
\]
++++
desde que o limite da direita exista. +
Na notação latexmath:[$x\to a$], latexmath:[$a$] pode ser finito ou infinito. Além disso,  latexmath:[$x\to a$] pode ser substituído pelos limites laterais latexmath:[$x\to 
a^-$] ou latexmath:[$x\to a^+$] 



.{zwsp}
====
Determinemos os limites da forma latexmath:[$\infty/\infty$]:

. latexmath:[$\lim \limits_{x\to \infty}\dfrac{e^x}{x^2}$]

Solução:: 

Desde que latexmath:[$\lim \limits_{x\to \infty}e^x=\infty$] e latexmath:[$\lim \limits_{x\to \infty}{x^2}=\infty$]
+
[latexmath]
++++
\[
\lim \limits_{x\to \infty}\dfrac{e^x}{x^2}=\lim \limits_{x\to \infty}\dfrac{e^x}{2x}=\lim \limits_{x\to \infty}\dfrac{e^x}{2}=\infty.
\]
++++

. latexmath:[$\lim \limits_{x\to \pi/2^+}\dfrac{{\rm tan}(x)-5}{{\rm sec}(x)+4}$]

Solução::

Desde que latexmath:[$\lim \limits_{x\to \pi/2^+}{\rm tan}(x)=\infty$] e latexmath:[$\lim \limits_{x\to \pi/2^+}{\rm sec}(x)=-\infty$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to \pi/2^+}\dfrac{{\rm tan}(x)-5}{{\rm sec}(x)+4}=\lim \limits_{x\to \pi/2^+}\dfrac{{\rm sec}^2(x)}{{\rm sec}(x){\rm tan}(x)}=\lim \limits_{x\to \to \pi/2^+}\dfrac{1}{{\rm sen}(x)}=1.
\]
++++

. latexmath:[$\lim \limits_{x\to \infty}\dfrac{\ln(x)}{4\sqrt{x}}$]

Solução::

Desde que latexmath:[$\lim \limits_{x\to\infty}\ln(x)=\infty$] e latexmath:[$\lim \limits_{x\to \infty}\sqrt{x}=\infty$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to \infty}\dfrac{\ln(x)}{\sqrt{x}}=\lim \limits_{x\to \infty}\dfrac{\dfrac{1}{x}}{\dfrac{2}{\sqrt{x}}}=\lim \limits_{x\to \to 0}\dfrac{2}{\sqrt{x}}=0.
\]
++++
====

*Forma Indeterminada latexmath:[$\infty\cdot 0$]:* Para determinar latexmath:[$\lim\limits_{x\to a}f(x)g(x)$], quando latexmath:[$\lim\limits_{x\to a}f(x)=\infty$] e latexmath:[$\lim\limits_{x\to a}g(x)=0$], a função latexmath:[$f(x)g(x)$] deve ser expressa de forma que adote uma das formas indeterminadas: latexmath:[$0/0$] ou latexmath:[$\infty/\infty$], em outras palavras:
[latexmath]
++++
\[
\lim\limits_{x\to a}f(x)g(x)=\lim\limits_{x\to a}\dfrac{f(x)}{\dfrac{1}{g(x)}} \quad \mbox{ou}\quad \lim\limits_{x\to a}f(x)g(x)=\lim\limits_{x\to a}\dfrac{g(x)}{\dfrac{1}{f(x)}}, 
\]
++++
e assim, podemos aplicar as regras estabelecidas, anteriormente.

.{zwsp}
====
Determinemos os limites da forma latexmath:[$\infty\cdot0$]:

. latexmath:[$\lim \limits_{x\to \infty}x^{1/4}{\rm sen}\left(\dfrac{1}{\sqrt{x}}\right)$]

Solução:: 

Desde que latexmath:[$\lim \limits_{x\to \infty}x^{1/4}=\infty$] e latexmath:[$\lim \limits_{x\to \infty}{\rm sen}\left(\dfrac{1}{\sqrt{x}}\right)=0$], fazendo latexmath:[$z=\dfrac{1}{\sqrt{x}}$] temos que latexmath:[$x\to \infty$] se, e somente se, latexmath:[$z\to 0^+$], logo
+
[latexmath]
++++
\[
\lim \limits_{x\to \infty}x^{1/4}{\rm sen}\left(\dfrac{1}{\sqrt{x}}\right)=\lim \limits_{z\to 0^+}\dfrac{{\rm sen}(z)}{\sqrt{z}}=\lim \limits_{z\to 0^+}\dfrac{\cos(z)}{\dfrac{1}{2\sqrt{z}}}=\lim \limits_{z\to 0^+}2\sqrt{z}\cos(z)=0.
\]
++++

. latexmath:[$\lim \limits_{x\to 0^+}\sqrt{x}\ln(x)$]

Solução::

Desde que latexmath:[$\lim \limits_{x\to0^+}\sqrt{x}=0$] e latexmath:[$\lim \limits_{x\to 0^+}\ln(x)=-\infty$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to 0^+}\sqrt{x}\ln(x)=\lim \limits_{x\to0^+}\dfrac{\ln(x)}{1/\sqrt{x}}=\lim \limits_{x\to \to 0^+}\dfrac{1/x}{-1/2\sqrt{x^3} }=\lim \limits_{x\to \to 0^+}(-2\sqrt{x})=0.
\]
++++
====

*Forma Indeterminada latexmath:[$\infty- \infty$]:* Para determinar latexmath:[$\lim\limits_{x\to a}(f(x)-g(x))$], quando latexmath:[$\lim\limits_{x\to a}f(x)=\infty$] e latexmath:[$\lim\limits_{x\to a}g(x)=\infty$], a função latexmath:[$f(x)-g(x)$] deve ser expressa da seguinte forma:
[latexmath]
++++
\[
\lim\limits_{x\to a}(f(x)-g(x))=\ln\left(e^{\lim\limits_{x\to a}(f(x)-g(x))}\right)=\ln \left(\dfrac{e^{-\lim\limits_{x\to a}g(x)}}{e^{-\lim\limits_{x\to a}f(x)}}\right)
\]
++++
e desta forma quando latexmath:[$x\to a$], este quociente toma a forma indeterminada latexmath:[$0/0$] e podemos aplicar a teoria já estudada.

.{zwsp}
====
Determinemos os limites da forma latexmath:[$\infty-\infty$]:

. latexmath:[$\lim \limits_{x\to \pi/2^-}({\rm tan}(x)-{\rm sec}(x))$]

Solução::

Desde que latexmath:[$\lim \limits_{x\to \pi/2^-}{\rm tan}(x)=\infty$] e latexmath:[$\lim \limits_{x\to \pi/2^-}{\rm sec}(x)=+\infty$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to \pi/2^-}({\rm tan}(x)-{\rm sec}(x))=\ln(\lim \limits_{x\to \pi/2^-}\dfrac{{\rm sec}^2(x)}{{\rm sec}(x){\rm tan}(x)})=\ln(\lim \limits_{x\to  \pi/2^-}\dfrac{1}{{\rm sen}(x)})=\ln(1)=0.
\]
++++

. latexmath:[$\lim \limits_{x\to \infty}\dfrac{\ln(x)}{4\sqrt{x}}$]

Solução::

Desde que latexmath:[$\lim \limits_{x\to\infty}\ln(x)=\infty$] e latexmath:[$\lim \limits_{x\to \infty}\sqrt{x}=\infty$], temos que 
+
[latexmath]
++++
\[
\lim \limits_{x\to \infty}\dfrac{\ln(x)}{\sqrt{x}}=\lim \limits_{x\to \infty}\dfrac{\dfrac{1}{x}}{\dfrac{2}{\sqrt{x}}}=\lim \limits_{x\to \to 0}\dfrac{2}{\sqrt{x}}=0.
\]
++++
====


Potências indeterminadas::

Os limites da forma latexmath:[$1^\infty$], latexmath:[$0^0$] e latexmath:[$\infty^0$] podem às vezes, ser tratados em função de um logaritmo. Que por sua vez pode ser  resolvido usando a *regra de L'Hôpital* Este procedimento pode ser justificado pela continuidade da função exponencial e o teorema que fala sobre a continuidade da composição de funções. Em outras palavras:

Proposição:: Se latexmath:[$\lim\limits_{x\to a}\ln(f(x))=L$], então
[latexmath]
++++
\[
\lim\limits_{x\to a}f(x)=\lim\limits_{x\to a}e^{\ln(f(x))}=e^L,
\]
++++
onde latexmath:[$a$] pode ser finito ou infinito.

.{zwsp}
====
Determinemos os seguintes limites:

. latexmath:[$\lim\limits_{x\to 0^+}(1+x^2)^{1/x^2}$]
Solução::
Notemos que esta indeterminação é da forma latexmath:[$1^\infty$]. Fazendo latexmath:[$f(x)=(1+x^2)^{1/x^2}$], determinemos latexmath:[$\lim\limits_{x\to 0^+}\ln(f(x))$]. Como
+
[latexmath]
++++
\[
\ln((1+x^2)^{1/x^2})=\dfrac{1}{x^2}\ln(1+x^2)=\dfrac{\ln(1+x^2)}{x^2}
\]
++++
+
pela regra de L'Hôpital temos que 
+
[latexmath]
++++
\[
\lim\limits_{x\to 0^+}\dfrac{\ln(1+x^2)}{x^2}=\lim\limits_{x\to 0^+}\dfrac{2x}{2x(1+x^2)}=1
\]
++++
+
Portanto, latexmath:[$\lim\limits_{x\to 0^+}(1+x^2)^{1/x^2}=e^1=e$].

. latexmath:[$\lim\limits_{x\to \infty}x^{1/x^2}$]

Solução::

Notemos que esta indeterminação é da forma latexmath:[$\infty^0$]. Fazendo latexmath:[$f(x)=x^{1/x^2}$], determinemos latexmath:[$\lim\limits_{x\to \infty}\ln(f(x))$]. Como
+
[latexmath]
++++
\[
\ln(x^{1/x^2})=\dfrac{1}{x^2}\ln(x)=\dfrac{\ln(x)}{x^2}
\]
++++
+
pela regra de L'Hôpital temos que 
+
[latexmath]
++++
\[
\lim\limits_{x\to \infty}\dfrac{\ln(x)}{x^2}=\lim\limits_{x\to \infty}\dfrac{1/x}{2x}=\lim\limits_{x\to \infty}\dfrac{1}{2x^2}=0
\]
++++
+
Portanto,  latexmath:[$\lim\limits_{x\to \infty}x^{1/x^2}=e^0=1$].
====

=== Atividades

.. Determine os ponto críticos, intervalos onde a função é crescente e decrescente, os máximos e mínimos relativos:
... latexmath:[$f(x)=x^4-14x^2-24x+1$];
... latexmath:[$f(x)= \dfrac{x+1}{x^2+x+1}$];
... latexmath:[$f(x)= 2-3x+x^3$];
... latexmath:[$f(x)= 1-(x-2)^{4/5}$];
... latexmath:[$f(x)= x \sqrt{1-x^2}$];
... latexmath:[$f(x)= x^2(1-x\sqrt{x})$];
... latexmath:[$f(x)= \dfrac{x^2+2x-23}{x-4}$];
... latexmath:[$f(x)= \dfrac{x}{1+x^2}$];
... latexmath:[$f(x)= \dfrac{1}{\ln\left(x^4+4x^3+30\right)}$];
... latexmath:[$f(x)= -x^2\sqrt{x^2+2}$];
... latexmath:[$f(x)= x-\ln(1-x)$];
... latexmath:[$f(x)= x-\ln(1+x^2)$];
... latexmath:[$f(x)= \sqrt[3\]{(x^2-a^2)^2}$];
... latexmath:[$f(x)= (x^2-2x)\ln(x)-\dfrac{3}{2}x^2+4x$];
... latexmath:[$f(x)= \dfrac{x}{x^2-6x-16}$];
... latexmath:[$f(x)= x\ln(x)$];
... latexmath:[$f(x)= 2e^{x^2-4x}$];
... latexmath:[$f(x)= \dfrac{x}{ \sqrt[3\]{x^2-4}}$];
... latexmath:[$f(x)= x(x-1)^2(x-3)^3$];
... latexmath:[$f(x)= x\ln^2(x)$];
... latexmath:[$f(x)= \dfrac{2{\rm arctan}(x)}{3}+ \dfrac{1}{3}{\rm arctan}\left(\dfrac{x}{1-x^2}\right)$];


.. Determine se o *Teorema de valor médio* é aplicável, no intervalos indicado. Caso afirmativo, encontre os valores que o verificam; caso contrário dê uma razão que justifique sua reposta.


... latexmath:[$f(x)= x^2+2x$], em latexmath:[$[-2,0\]$];
... latexmath:[$f(x)=\sqrt{x^2+9} $], em latexmath:[$[0,4\]$];
... latexmath:[$f(x)= 2x^3-x^2-3x+5$], em latexmath:[$[-2,2\]$];
... latexmath:[$f(x)=\dfrac{x+1}{x-1} $], em latexmath:[$[2,4\]$];
... latexmath:[$f(x)= |4-x^2|$], em latexmath:[$[-2,2\]$];
... latexmath:[$f(x)=|9-4x^2|$], em latexmath:[$\left [ -\dfrac{3}{2} ,\dfrac{3}{2}\right\]$];
... latexmath:[$f(x)= \left\{ \begin{array}{ll}
	2x+3& \mbox{se }x<3\\
	15-2x& \mbox{se }x\geq3
\end{array}\right.$], em latexmath:[$[-1,5\]$];


... latexmath:[$f(x)= \left\{ \begin{array}{ll}
	\dfrac{3-x^2}{2}& \mbox{se }x\leq1\\
	\dfrac{1}{x}& \mbox{se }x>1
\end{array}\right.$], em latexmath:[$[0,2\]$];
... latexmath:[$f(x)= \left\{ \begin{array}{ll}
	x^2+4& \mbox{se }-2\leq x<0\\
4-x^3& \mbox{se }0\leq x<1\\
	\dfrac{6}{x^2+1}& \mbox{se }1\leq x\leq 2
\end{array}\right.$], em latexmath:[$[-2,2\]$];
... latexmath:[$f(x)= \left\{ \begin{array}{ll}
	\dfrac{4}{x^2}& \mbox{se }x\leq-1\\
	8-4x^2& \mbox{se }x>-1
\end{array}\right.$], em latexmath:[$[-2,0\]$];
... latexmath:[$f(x)= \left\{ \begin{array}{ll}
	|x^2+9|& \mbox{se } x<2\\
5+2\sqrt{x-2}& \mbox{se }2\leq x\leq 11\\
1+(x-11)^3& \mbox{se } x>11
\end{array}\right.$], em latexmath:[$[-4,12\]$];
... latexmath:[$f(x)= \dfrac{|x|^3}{1 + x^6}$], em latexmath:[$[-2,2\]$];
... latexmath:[$f(x)= \dfrac{x^3}{x^2-4}$], em latexmath:[$[-9,-4\]$];
... latexmath:[$f(x)=\dfrac{x^2}{4+|x|} $], em latexmath:[$[-1,2\]$];



.. Construa os gráficos das funções indicando, os pontos de descontinuidade, os pontos críticos, intervalos onde é crescente e decrescente, os máximos e mínimos relativos, os pontos de inflexão e intervalos de concavidade:

... latexmath:[$f(x)=3x^4+4x^3+6x^2-4 $];
... latexmath:[$f(x)= x^2(x+4)^3$];
... latexmath:[$f(x)= x^4-3x^3+3x^2+1$];
... latexmath:[$f(x)= \dfrac{x^2}{x-1}$];
... latexmath:[$f(x)= 3x^{2/3}-2x$];
... latexmath:[$f(x)= (x+2)\sqrt{-x}$];
... latexmath:[$f(x)= x-\ln(x+1)$];
... latexmath:[$f(x)= \dfrac{x^3}{3-x^2}$];
... latexmath:[$f(x)= x-{\rm arctan}(x)$];
... latexmath:[$f(x)= \dfrac{2{\rm arctan}(x)}{3} + \dfrac{1}{3}{\rm arctan}\left(\dfrac{x}{1-x^2}\right) $];
... latexmath:[$f(x)= \dfrac{(x-1)^2}{(x+1)^3}$];
... latexmath:[$f(x)= \dfrac{2{\rm arcsen}(x)}{\sqrt{1-x^2}}$];
... latexmath:[$f(x)= \cos(x)\cos(2x)$];
... latexmath:[$f(x)= {\rm sen}(x)+\cos(x)$];
... latexmath:[$f(x)= {\rm sen}^3(x)+\cos^3(x)$];
... latexmath:[$f(x)= \dfrac{\ln\left(\sqrt{x^2+1}\right)-1}{x}$];
... latexmath:[$f(x)= (x+1)\ln^2(x+1)$];
... latexmath:[$f(x)= \dfrac{x^2}{2}\ln\left(\dfrac{x}{x}\right)$];
... latexmath:[$f(x)= (x^2+2)e^{-x^2}$];
... latexmath:[$f(x)= \dfrac{x}{ \sqrt[3\]{x^2-1}}$];
... latexmath:[$f(x)= 2x+2-3\sqrt[3\]{(x+2)^2}$];
... latexmath:[$f(x)= \sqrt{8+x}-\sqrt{8-x}$];
... latexmath:[$f(x)= \dfrac{4x-12}{(x-2)^2}$];
... latexmath:[$f(x)= \dfrac{x+4}{2}\sqrt[3\]{x-4}$];
... latexmath:[$f(x)= \dfrac{x^2-3x-4}{x-2}$];
... latexmath:[$f(x)= 4x^5-5x^4$];
... latexmath:[$f(x)= 2(18x+6x^2-2x^3-54)^{1/3}$];
... latexmath:[$f(x)= {\rm arctan}(\ln(x))$];
... latexmath:[$f(x)= \ln(3x-x^2)$];
... latexmath:[$f(x)= e^{-x}\cos(x)$];



.. Determine os seguintes limites aplicando a regra de L'Hôpital:

... latexmath:[$\lim\limits_{x\to 0}\dfrac{x-{\rm sen}(x)}{x-{\rm tan}(x)}$];
... latexmath:[$\lim\limits_{x\to \infty}\dfrac{\pi-2{\rm arctan}(x)}{\ln\left(1+\dfrac{1}{x}\right)}$];
... latexmath:[$\lim\limits_{x\to a}\dfrac{x^m-a^m}{x^n-a^n}$];
... latexmath:[$\lim\limits_{x\to 0}\dfrac{e^{x^2}-1}{\cos(x)-1}$];
... latexmath:[$\lim\limits_{x\to 0}\dfrac{e^x-e^{-x}}{{\rm sen}(x)\cos(x)}$];
... latexmath:[$\lim\limits_{x\to 0}\dfrac{e^x-e^{-x}-2x}{x-{\rm sen}(x)}$];
... latexmath:[$\lim\limits_{x\to 0}\dfrac{e^x- \dfrac{x^3}{6} - \dfrac{x^2}{2}-x-1 }{\cos(x)+\dfrac{x^2}{2}-1}$];
... latexmath:[$\lim\limits_{x\to 0}\dfrac{\ln(1+x)^4-4x+2x^2-\dfrac{4}{3}x^3 +x^4 }{6{\rm sen}(x)-6x+x^3}$];
... latexmath:[$\lim\limits_{x\to 1}\left( \dfrac{x}{x-1}- \dfrac{1}{\ln(x)}\right)$];
... latexmath:[$\lim\limits_{x\to 1}\left(  \dfrac{1}{\ln(x)}-\dfrac{x}{\ln(x)}-\right)$];
... latexmath:[$\lim\limits_{x\to 0}\left(\dfrac{1}{x}\right)^{{\rm sen}(x)}$];
... latexmath:[$\lim\limits_{x\to 0}x^x$];
... latexmath:[$\lim\limits_{x\to 1}x^{\dfrac{1}{1-x}}$];
... latexmath:[$\lim\limits_{x\to \pi/2}({\rm sen}(x))^{{\rm tan}(x)}$];







